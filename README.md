# Portfolio

This folder contains a selection of my projects. This is a more curated list of the projects on my github making some adjustments and fixing some documentation. I keep the repository on my separate personal github private, so I created this folder to put together some highlights from my recent projects. Since some of the projects (especially for the projects that are based on course projects) they have specs that live outside of the projects themselves and are sometimes mostly for learning purposes, I have added some basic READMEs to make more clear what the intent of them is. For transparency I wanted to note that I have used the help of AI to make the process of creating the READMEs more quickly.

This is a collection of projects from personal work and course workd from school.

## Personal Projects
These are projects I implemented from scratch either for learning purposes or personal use.

| Project | Description | Tech |
|---|---|---|
| `quote_cli` | Universal quote scraper and manager | Python |
| `my_shell_project` | Custom Unix shell in C | C |
| `money_stats` | Newsletter analytics dashboard | HTML/CSS/JS |
| `eco_search_engine` | Semantic search engine with fine-tuned MiniLM | Python |

## Course Projects
These are projects I implemented as part of a course in undergrad. Some are purely the material from the course while others were expanded based on some personal interests to improve the projects.

| Project | Description | Tech |
|---|---|---|
| `avl_implementation` | Self-balancing AVL tree | C++ |
| `bank_simulator` | Discrete-event bank simulation | C++ |
| `cnn_deep_learning` | CNN image classifier with transfer learning | PyTorch |
| `custom_string_class` | Raw pointer string library | C++ |
| `database_schema_design` | Relational schema with SQL | SQL |
| `hash_table_implementation` | Templated hash table with open addressing | C++ |
| `icu_mortality_prediction` | ML pipeline for ICU mortality | Python/sklearn |
| `information_retrieval_pipeline` | Text preprocessing, TF-IDF, crawler, PageRank | Python |
| `MST_FASTTSP_OPTTSP` | MST and TSP solvers | C++ |
| `naive_bayes_classifier` | Text classifier from scratch | C++ |
| `web_rest_api` | JSON REST API with custom linked list | C++ |

## Highlights
1. As a fan of Matt Levine's newsletter the `money_stats` project has been my favorite since it is a great way to directly apply some of the visualization practices from my work directly to data that I find interesting. My goal with this project is to keep recording these stats as they evolve over the coming years since for now the data only partains to 2025.
2. As a fan of the italian TV show Boris I wanted to have a way to create a random generator that could spit out a quote from the show whenever I wanted some inspiration. I then expanded this to a more general CLI tool for terminal-local use to be usable with other sets of quotes.
3. Another major project that I am not including in this Portfolio folder because it would be hard to showcase are my set-up for my dotfiles. These include non-trivial neovim config files, tmux and hyprland config files, and more.
